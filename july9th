//Alert the sum of the second lowest and the second highest number

const array = [1,2,0,3,4,5,6]
let max= -Infinity, secondMax = -Infinity;
console.log(1>undefined)
array.sort()
console.log(array)
for(let i = 0; i<array.length; i++){
    if(array[i]>max){
        secondMax = max;
        max = array[i];
    }
}
console.log('sum of second lowest and second highest is: ', secondMax + array[1])

https://www.youtube.com/watch?v=5pj8xNFVuWw explanation
learnings: 1. js doesnt have negative indices like python array[-2] is undefined.
2. any comparision with NaN is false. undefined <1 undefined typecasts to NaN so false.
3. array.sort() will manipulate the original array
4. cant auto inititalize 3 variables a=b=c=0 like python
